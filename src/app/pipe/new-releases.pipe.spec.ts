import { NewReleasesPipe } from './new-releases.pipe';

describe('NewReleasesPipe', () => {
  it('create an instance', () => {
    const pipe = new NewReleasesPipe();
    expect(pipe).toBeTruthy();
  });
});
